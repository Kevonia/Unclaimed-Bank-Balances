version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - .:/app
    environment:
      - DASH_DEBUG_MODE=True  # Only for development
    command: python main.py